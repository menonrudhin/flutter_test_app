import 'package:flutter/material.dart';

// First Banner
class TheExperience extends StatelessWidget {
  Widget build(BuildContext context){
    return Column(
      mainAxisAlignment: MainAxisAlignment.center,
      crossAxisAlignment: CrossAxisAlignment.stretch,
      children: [
        Text(
          'The Experience'
        )
      ],
    );
  }
}
